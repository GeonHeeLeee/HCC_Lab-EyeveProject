server:
  port: 7942

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/kw_db?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: rootroot
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update

logging:
  level:
    root: info
    com.example: debug